[package]
name = "cloudjet-rust-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
lapin = "2"

serde = { version = "1", features = ["derive"] }

serde_json = "1"

futures-util = "0.3"